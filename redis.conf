# Redis 配置
port 6379
bind 0.0.0.0
requirepass ai-studio-redis

# 内存限制（1GB 缓存 + 512MB 会话）
maxmemory 1536mb
maxmemory-policy allkeys-lru

# 持久化
save 900 1
save 300 10
save 60 10000
appendonly yes
appendfsync everysec

# 慢日志
slowlog-log-slower-than 10000
slowlog-max-len 128
